# ================================
# Backend (.env) de exemplo
# Renomeie este arquivo para .env e
# preencha com os valores reais
# ================================

# Banco de Dados (MySQL - Railway ou local)
# Você pode usar UMA das variáveis abaixo (a aplicação aceita ambas):
# 1) MYSQL_URL     -> Ex.: mysql://usuario:senha@host:porta/railway
# 2) DATABASE_URL  -> Ex.: mysql://usuario:senha@host:porta/railway
# Se estiver usando Railway, cole aqui a URL completa que ele fornece.
MYSQL_URL=
DATABASE_URL=
DB_SSL=false

# Servidor
NODE_ENV=development
PORT=5000

# Autenticação
JWT_SECRET=sua_chave_secreta_super_segura
JWT_EXPIRES_IN=7d

# CORS / Frontend autorizado
FRONTEND_URL=https://seu-frontend.vercel.app

# Rate limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100